export enum MilestoneStatus {
  ACTIVE = "active",
  COMPLETED = "completed",
  ARCHIVED = "archived",
}

export enum MilestoneActionType {
  CREATE = "create",
  UPDATE = "update",
  STATUS_CHANGE = "status_change",
  DELETE = "delete",
}
